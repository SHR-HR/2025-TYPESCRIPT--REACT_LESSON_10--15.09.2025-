// Импорт типа User из файла '../types/User' для типизации данных пользователей
import type { User } from '../types/User';

// Экспорт массива users с типом User[] (массив объектов User)
// Массив содержит тестовые/моковые данные пользователей для разработки
export const users: User[] = [
  // Первый объект пользователя в массиве
  {
    // Уникальный идентификатор пользователя (числовой тип)
    id: 1,
    // Полное имя пользователя (строковый тип)
    name: 'Алексей Иванов',
    // Электронная почта пользователя (строковый тип)
    email: 'alexey@example.com'
  },

  // Второй объект пользователя в массиве
  {
    // Уникальный идентификатор пользователя (числовой тип)
    id: 2,
    // Полное имя пользователя (строковый тип)
    name: 'Мария Петрова',
    // Электронная почта пользователя (строковый тип)
    email: 'maria@example.com'
  },

  // Третий объект пользователя в массиве
  {
    // Уникальный идентификатор пользователя (числовой тип)
    id: 3,
    // Полное имя пользователя (строковый тип)
    name: 'Дмитрий Сидоров',
    // Электронная почта пользователя (строковый тип)
    email: 'dmitry@example.com'
  },
];

/*
===========================================
ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
===========================================

1. Файл users.ts - файл с моковыми (тестовыми) данными пользователей

2. Комментарий "Импорт типа User из файла '../types/User'..." - объясняет
   импорт типа для обеспечения строгой типизации данных пользователей

3. Комментарий "Экспорт массива users с типом User[]..." - описывает
   экспорт массива пользователей с указанием типа массива

4. Комментарий "Массив содержит тестовые/моковые данные пользователей..." - поясняет
   назначение данных - для разработки и тестирования без реального сервера

5. Комментарий "Первый объект пользователя в массиве..." - маркирует
   начало описания первого элемента массива

6. Комментарий "Уникальный идентификатор пользователя (числовой тип)..." - описывает
   свойство id как числовой уникальный идентификатор

7. Комментарий "Полное имя пользователя (строковый тип)..." - поясняет
   свойство name как строковое значение полного имени

8. Комментарий "Электронная почта пользователя (строковый тип)..." - объясняет
   свойство email как строковое значение email адреса

ОСОБЕННОСТИ РЕАЛИЗАЦИИ:

- Массив users экспортируется как константа
- Все объекты соответствуют типу User (имеют id, name, email)
- ID пользователей являются последовательными числами (1, 2, 3)
- Имена пользователей представлены в русской локали
- Email адреса используют домен example.com (стандартный для тестовых данных)

СТРУКТУРА ДАННЫХ:

users - массив объектов User:
[
  {
    id: number,      // Уникальный числовой идентификатор
    name: string,    // Полное имя пользователя
    email: string    // Адрес электронной почты
  },
  ...
]

ИСПОЛЬЗОВАНИЕ:
- Тестовые данные для разработки компонентов
- Заполнение интерфейса при отсутствии бэкенда
- Демонстрационные данные для примеров
- Тестирование функционала работы с пользователями
- Временная замена API запросов на начальном этапе разработки
*/