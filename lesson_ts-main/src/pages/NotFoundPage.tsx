// Импорт необходимых модулей и компонентов React
import React from 'react';
// Импорт компонента Link из react-router-dom для навигации
import { Link } from 'react-router-dom';
// Импорт главного layout компонента для структуры страницы
import MainLayoute from '../layouts/MainLayoute';

// Определение функционального компонента NotFoundPage с типизацией React.FC
const NotFoundPage: React.FC = () => {
    // Возвращаем JSX разметку компонента
    return (
        // Используем главный layout для оформления страницы
        <MainLayoute>
            {/* Основной контейнер страницы */}
            <div className="container">
                {/* Заголовок страницы с кодом ошибки 404 */}
                <h1>404 - Страница не найдена</h1>
                {/* Ссылка для возврата на главную страницу */}
                <Link to="/">Вернуться на главную</Link>
            </div>
        </MainLayoute>
    );
};

// Экспорт компонента по умолчанию
export default NotFoundPage;

/*
===========================================
ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
===========================================

1. Файл NotFoundPage.tsx - React компонент страницы 404 (не найдено)

2. Комментарий "Импорт необходимых модулей и компонентов React" - описывает
   базовый импорт React для создания функционального компонента

3. Комментарий "Импорт компонента Link из react-router-dom для навигации" - поясняет
   использование компонента Link для создания кликабельных ссылок без перезагрузки страницы

4. Комментарий "Импорт главного layout компонента для структуры страницы" - описывает
   использование компонента MainLayoute для обеспечения一致的ного оформления страницы

5. Комментарий "Определение функционального компонента NotFoundPage с типизацией React.FC" - объясняет
   создание компонента с TypeScript типизацией React Functional Component

6. Комментарий "Возвращаем JSX разметку компонента" - маркирует
   начало возвращаемой JSX структуры компонента

7. Комментарий "Используем главный layout для оформления страницы" - описывает
   обертку контента в компонент макета для сохранения общей структуры приложения

8. Комментарий "Основной контейнер страницы" - поясняет
   использование контейнера для центрирования и ограничения контента

9. Комментарий "Заголовок страницы с кодом ошибки 404" - описывает
   основной заголовок страницы, информирующий пользователя об ошибке

10. Комментарий "Ссылка для возврата на главную страницу" - объясняет
    предоставление пользователю возможности вернуться на главную страницу приложения

11. Комментарий "Экспорт компонента по умолчанию" - описывает
    стандартный экспорт компонента для использования в других частях приложения

ОСОБЕННОСТИ РЕАЛИЗАЦИИ:

- Минималистичная структура страницы ошибки 404
- Использование компонента Link для клиентской навигации
- Интеграция с общим layout приложения для сохранения一致性 стиля
- Четкое информирование пользователя о проблеме
- Предоставление простого способа вернуться к основному контенту

СТРУКТУРА КОМПОНЕНТА:

- Обертка в главный layout компонент
- Контейнер для центрирования контента
- Информационный заголовок о ошибке 404
- Навигационная ссылка на главную страницу

ИСПОЛЬЗОВАНИЕ:
- Страница отображается при попытке перехода на несуществующий маршрут
- Обработка ошибок навигации в React Router
- Предоставление пользователю友好的 интерфейса при возникновении ошибки 404
- Сохранение общей навигационной структуры приложения даже на странице ошибки
*/