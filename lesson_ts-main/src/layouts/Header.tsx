// Импорт компонента Navigation из указанного пути для использования в компоненте Header
import Navigation from "../features/Navigation/Navigation";

// Объявление функционального компонента Header
function Header() {
  // Возвращение JSX разметки компонента Header
  return (
    // HTML-элемент header, который представляет собой шапку сайта
    <header>
      {/* Вставка компонента Navigation внутрь элемента header */}
      <Navigation />
    </header>
  );
}

// Экспорт компонента Header по умолчанию для возможности его использования в других частях приложения
export default Header;

/*
===========================================
ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
===========================================

1. Файл Header.tsx - компонент шапки сайта

2. Комментарий "Импорт компонента Navigation из указанного пути для использования в компоненте Header" - 
   объясняет назначение импорта компонента Navigation, который отвечает за навигацию по сайту

3. Комментарий "Объявление функционального компонента Header" - 
   указывает на создание функционального React-компонента с именем Header

4. Комментарий "Возвращение JSX разметки компонента Header" - 
   обозначает начало возвращаемой JSX структуры компонента

5. Комментарий "HTML-элемент header, который представляет собой шапку сайта" - 
   описывает семантический HTML-элемент header, используемый для создания шапки веб-страницы

6. Комментарий "Вставка компонента Navigation внутрь элемента header" - 
   поясняет размещение компонента навигации внутри шапки сайта

7. Комментарий "Экспорт компонента Header по умолчанию для возможности его использования в других частях приложения" - 
   объясняет экспорт компонента, чтобы его можно было импортировать и использовать в других файлах приложения

ОСОБЕННОСТИ РЕАЛИЗАЦИИ:

- Компонент Header является простым контейнером для компонента Navigation
- Использует семантический HTML-тег header для улучшения доступности и SEO
- Делегирует всю навигационную логику компоненту Navigation
- Следует принципу единственной ответственности - отвечает только за отображение шапки

СТРУКТУРА КОМПОНЕНТА:

- Корневой элемент <header> - семантический контейнер для шапки
- Вложенный компонент <Navigation/> - отвечает за навигационное меню

ИСПОЛЬЗОВАНИЕ:
- Создает шапку сайта с навигационным меню
- Обеспечивает семантическую структуру HTML-документа
- Может быть использован в layout-компонентах для создания общей структуры страницы
- Упрощает композицию компонентов через вложенность
*/