// Импорт функции defineConfig из Vite для типизированной конфигурации
import { defineConfig } from 'vite'
// Импорт плагина react из @vitejs/plugin-react для поддержки React
import react from '@vitejs/plugin-react'

// Базовый URL конфигурации документации Vite: https://vite.dev/config/
// Экспорт конфигурации по умолчанию с использованием defineConfig
export default defineConfig({
  // Массив плагинов Vite, которые будут использоваться в проекте
  plugins: [react()], // Подключение плагина React для обработки .jsx и .tsx файлов
})

/*
===========================================
ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
===========================================

1. Файл vite.config.ts - файл конфигурации для сборщика Vite

2. Комментарий "Импорт функции defineConfig из Vite..." - объясняет назначение
   импортируемой функции для создания типизированной конфигурации

3. Комментарий "Импорт плагина react из @vitejs/plugin-react..." - описывает
   импорт официального плагина Vite для поддержки React

4. Комментарий "Базовый URL конфигурации документации Vite..." - предоставляет
   ссылку на официальную документацию для дополнительной информации

5. Комментарий "Экспорт конфигурации по умолчанию..." - поясняет экспорт
   конфигурации как модуля по умолчанию

6. Комментарий "Массив плагинов Vite..." - описывает структуру свойства plugins,
   которое содержит массив подключаемых плагинов

7. Комментарий "Подключение плагина React для обработки..." - детализирует
   назначение плагина react() для работы с JSX/TSX файлами

ТЕХНИЧЕСКИЕ ДЕТАЛИ:
- Vite: современный быстрый сборщик для веб-приложений
- defineConfig: функция для TypeScript-поддержки и автодополнения
- @vitejs/plugin-react: официальный плагин для React с горячей перезагрузкой
- Плагины: преобразуют и обрабатывают файлы во время разработки и сборки

НАЗНАЧЕНИЕ ФАЙЛА:
Конфигурационный файл Vite определяет настройки сборки, плагины и поведение
девелопмент-сервера для React/TypeScript приложения
*/


