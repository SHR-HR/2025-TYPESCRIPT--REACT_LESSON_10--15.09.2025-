// Импорт базовой конфигурации ESLint из официального пакета @eslint/js
import js from '@eslint/js'
// Импорт глобальных переменных для различных сред (браузер, node и т.д.)
import globals from 'globals'
// Импорт плагина ESLint для правил React Hooks (useState, useEffect и т.д.)
import reactHooks from 'eslint-plugin-react-hooks'
// Импорт плагина ESLint для поддержки React Refresh (горячее обновление)
import reactRefresh from 'eslint-plugin-react-refresh'
// Импорт интеграции TypeScript с ESLint для проверки TypeScript кода
import tseslint from 'typescript-eslint'
// Импорт функции для глобального игнорирования файлов и директорий
import { globalIgnores } from 'eslint/config'

// Экспорт конфигурации ESLint по умолчанию
// Конфигурация представляет собой массив объектов конфигурации
export default tseslint.config([
  // Глобальное игнорирование директории 'dist' (собранные файлы)
  globalIgnores(['dist']),

  // Основной объект конфигурации для TypeScript и React файлов
  {
    // Шаблон файлов к которым применяется эта конфигурация
    // Все файлы с расширениями .ts и .tsx (TypeScript и TypeScript + JSX)
    files: ['**/*.{ts,tsx}'],

    // Массив расширений (предустановленных конфигураций) которые наследуются
    extends: [
      // Рекомендуемая конфигурация из базового пакета ESLint
      js.configs.recommended,
      // Рекомендуемая конфигурация для TypeScript от typescript-eslint
      tseslint.configs.recommended,
      // Последняя рекомендуемая конфигурация для React Hooks
      reactHooks.configs['recommended-latest'],
      // Конфигурация для React Refresh в Vite окружении
      reactRefresh.configs.vite,
    ],

    // Настройки языка и окружения
    languageOptions: {
      // Версия ECMAScript (2020 - современный стандарт)
      ecmaVersion: 2020,
      // Глобальные переменные браузерного окружения (window, document, console и т.д.)
      globals: globals.browser,
    },
  },
])

/*
===========================================
ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
===========================================

1. Файл eslint.config.js - конфигурационный файл ESLint для линтинга кода

2. Комментарий "Импорт базовой конфигурации ESLint..." - объясняет импорт
   официальной рекомендуемой конфигурации ESLint

3. Комментарий "Импорт глобальных переменных для различных сред..." - описывает
   пакет который предоставляет глобальные переменные для разных окружений

4. Комментарий "Импорт плагина ESLint для правил React Hooks..." - поясняет
   плагин для проверки правильного использования React хуков

5. Комментарий "Импорт плагина ESLint для поддержки React Refresh..." - описывает
   плагин для работы с горячим обновлением React компонентов

6. Комментарий "Импорт интеграции TypeScript с ESLint..." - объясняет пакет
   который позволяет ESLint работать с TypeScript кодом

7. Комментарий "Импорт функции для глобального игнорирования..." - описывает
   функцию для исключения файлов и папок из проверки ESLint

8. Комментарий "Экспорт конфигурации ESLint по умолчанию..." - поясняет
   экспорт конфигурации как модуля по умолчанию

9. Комментарий "Конфигурация представляет собой массив..." - описывает структуру
   современного ESLint конфигурационного файла

10. Комментарий "Глобальное игнорирование директории 'dist'..." - объясняет
    исключение папки со скомпилированными файлами из проверки

11. Комментарий "Основной объект конфигурации для TypeScript..." - описывает
    главный блок конфигурации для TypeScript/React файлов

12. Комментарий "Шаблон файлов к которым применяется эта конфигурация..." - 
    поясняет glob pattern для выбора обрабатываемых файлов

13. Комментарий "Массив расширений (предустановленных конфигураций)..." - 
    описывает наследование от рекомендуемых конфигураций

14. Комментарий "Рекомендуемая конфигурация из базового пакета ESLint..." - 
    указывает на стандартные правила ESLint

15. Комментарий "Рекомендуемая конфигурация для TypeScript..." - описывает
    правила специфичные для TypeScript

16. Комментарий "Последняя рекомендуемая конфигурация для React Hooks..." - 
    поясняет правила для корректного использования React хуков

17. Комментарий "Конфигурация для React Refresh в Vite окружении..." - 
    описывает настройки для горячего обновления в Vite

18. Комментарий "Настройки языка и окружения..." - раздел с настройками
    JavaScript/TypeScript окружения

19. Комментарий "Версия ECMAScript (2020 - современный стандарт)..." - 
    указывает версию JavaScript для которой написаны правила

20. Комментарий "Глобальные переменные браузерного окружения..." - описывает
    добавление глобальных переменных браузера (window, document и т.д.)

ТЕХНИЧЕСКИЕ ДЕТАЛИ:
- ESLint: линтер для JavaScript/TypeScript кода
- Конфигурация в формате ES модулей (современный подход)
- Наследование от рекомендуемых конфигураций для лучших практик
- Специфичные правила для React и TypeScript
- Игнорирование папки dist чтобы не линтить скомпилированный код

НАЗНАЧЕНИЕ ФАЙЛА:
Обеспечение качества кода через статический анализ и проверку
соответствия стандартам кодирования для TypeScript и React проекта
*/