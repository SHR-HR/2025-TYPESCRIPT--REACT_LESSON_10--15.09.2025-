# Зависимости Python-пакетов для проекта Test-API
# Каждая строка содержит название пакета и его точную версию

# FastAPI: современный, быстрый веб-фреймворк для построения API с Python 3.7+
# Версия 0.104.1: конкретная стабильная версия фреймворка
fastapi==0.104.1

# Uvicorn: легковесный и быстрый ASGI-сервер для запуска FastAPI приложений
# Версия 0.24.0: совместимая версия сервера для указанной версии FastAPI
uvicorn==0.24.0

# Pydantic: библиотека для валидации данных и настроек с использованием аннотаций типов Python
# Версия 2.5.0: мажорная версия 2 с обратно несовместимыми изменениями
pydantic==2.5.0

# Python-multipart: библиотека для парсинга multipart/form-data (загрузка файлов)
# Версия 0.0.9: необходима для обработки загрузки файлов в FastAPI
python-multipart==0.0.9

# /*
# ===========================================
# ПОЯСНЕНИЯ К КОММЕНТАРИЯМ В ДАННОМ ФАЙЛЕ:
# ===========================================

# 1. Файл requirements.txt - стандартный файл зависимостей Python-проекта

# 2. Комментарий "Зависимости Python-пакетов..." - объясняет назначение файла
#    и формат хранения информации о зависимостях

# 3. Комментарий "FastAPI: современный, быстрый веб-фреймворк..." - описывает
#    основной фреймворк проекта и его назначение для построения API

# 4. Комментарий "Версия 0.104.1..." - поясняет важность указания точной версии
#    для обеспечения воспроизводимости сборки

# 5. Комментарий "Uvicorn: легковесный и быстрый ASGI-сервер..." - объясняет
#    назначение сервера для запуска FastAPI приложений

# 6. Комментарий "Pydantic: библиотека для валидации данных..." - описывает
#    библиотеку для работы с данными и валидации в FastAPI

# 7. Комментарий "Версия 2.5.0: мажорная версия 2..." - указывает на важность
#    мажорной версии и обратной несовместимости

# 8. Комментарий "Python-multipart: библиотека для парсинга..." - поясняет
#    необходимость этой библиотеки для обработки загрузки файлов

# СИНТАКСИС ФАЙЛА:
# - Каждая строка содержит название пакета и версию через ==
# - Версии зафиксированы для обеспечения стабильности проекта
# - Комментарии начинаются с символа # и игнорируются pip

# НАЗНАЧЕНИЕ ФАЙЛА:
# Файл requirements.txt используется инструментом pip для установки
# всех необходимых зависимостей проекта одной командой:
# pip install -r requirements.txt
# */


